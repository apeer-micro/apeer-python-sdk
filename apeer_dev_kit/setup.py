from setuptools import setup

setup(name='APEER Python SDK',
      version='1.0.0',
      description='Development kit for creating modules on apeer',
      license='MIT',
      packages=['apeer-dev-kit'],
      zip_safe=False)
