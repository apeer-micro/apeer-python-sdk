from setuptools import setup

setup(name='ianus',
      version='1.0.0',
      description='Development kit for creating modules on apeer',
      license='MIT',
      packages=['ianus'],
      zip_safe=False)
